 {{- if eq .Values.service.type "Route" }}

apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: guestbook
    tier: backend
  name: backend
  namespace: guestbook
spec:
  port:
    targetPort: 8080
  to:
    kind: Service
    name: backend

  {{- end }}
